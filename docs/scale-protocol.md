# Felicita Arc BLE 프로토콜

> **2026-08-06 실물 검증 완료** (기기명 `FELICITA`). 아래 값은 실측 기준입니다.
> **무게 단위는 0.01 g입니다** — 인터넷에 도는 자료 다수가 0.1 g으로 적고 있으니 그쪽에 맞춰 되돌리지 마세요.

## GATT (실물 확인됨)

| 항목 | 값 |
|---|---|
| Service UUID | `0000ffe0-0000-1000-8000-00805f9b34fb` |
| Characteristic UUID | `0000ffe1-0000-1000-8000-00805f9b34fb` |
| Characteristic 속성 | `read, write, writeWithoutResponse, notify` |
| Heartbeat | 불필요 (검증 세션 동안 연결 유지 확인) |

## 무게 패킷 (실물 확인됨)

실측 캡처 3건:

```
398.9 g   01 02 2b 30 33 39 38 39 30 20 67 43 68 4d 22 89 0d 0a
-341.1 g  01 02 2d 30 33 34 31 31 30 20 67 43 21 16 22 89 0d 0a
  0.0 g   01 02 2b 30 30 30 30 30 30 20 67 43 e3 4f 22 88 0d 0a
          └헤더┘ ±  └── 숫자 6자리 ──┘ ␣  g  ─ 미상 ─  └CRLF┘
```

> ⚠️ **저울은 같은 characteristic으로 무게 외의 패킷도 보냅니다.**
> 타이머·모드 명령을 보낸 뒤 index 0~1에 `01 02`가 아닌 값이 관측됐습니다(총 3종).
> **반드시 헤더 `01 02`로 걸러낸 뒤 파싱하세요.** 걸러내지 않으면 다른 패킷을 무게로 잘못 읽어
> 곡선에 튀는 점이 생깁니다. 비-무게 패킷의 구조는 아직 분석하지 않았습니다(현재 불필요).

| index | 내용 | 비고 |
|---|---|---|
| 0~1 | `01 02` 헤더 | **무게 패킷 식별자.** 다른 값이면 무게 패킷이 아님 |
| 2 | 부호 — `0x2B`(+) / `0x2D`(−) | |
| 3~8 | ASCII 숫자 6자리 | **단위 0.01 g → 정수 파싱 후 100으로 나눔** |
| 9~10 | 공백 + `'g'` | 단위 문자, 고정 |
| 11, 14 | `0x43`, `0x22` | 두 캡처에서 동일, 의미 미상 |
| 12, 13, 15 | 패킷마다 다름 | **무게와 무관** — 체크섬·타이머·배터리 추정. 파서는 무시합니다 |
| 16~17 | `0D 0A` (CRLF) | 종료 |

저울 해상도가 0.1 g이라 **마지막 숫자(index 8)는 항상 `'0'`**입니다. 0.01 g 단위 해석의 방증입니다.
2 kg까지 측정하는 모델이므로 숫자부는 최대 `200000`(= 2000.00 g)까지 커질 수 있고, 그때 index 3도 변합니다.

파서 정식 구현은 [`frontend/src/ble/parseWeight.ts`](../frontend/src/ble/parseWeight.ts)이며, 위 실측 패킷을 fixture로 한 단위 테스트가 있습니다.

## 명령

| 명령 | Byte | 기능 |
|---|---:|---|
| `T` | `0x54` | 영점 |
| `R` | `0x52` | 타이머 시작 |
| `S` | `0x53` | 타이머 정지 |
| `C` | `0x43` | 타이머 리셋 |
| `2` | `0x32` | weight + timer 모드 |

## 실물 검증 체크리스트

검증에는 [`tools/hex-dump.html`](../tools/hex-dump.html)을 씁니다. 브라우저로 열면 되고 설치·빌드가 필요 없습니다.
`file://`로 열어도 동작하지만 안 되면 레포 루트에서 `python -m http.server 8080` 후 `http://localhost:8080/tools/hex-dump.html`로 접속하세요.

**"바이트별 변화 분석"이 핵심입니다.** 저울에 물을 조금씩 올리면서 파란색으로 바뀌는 인덱스를 보면, 아래 오프셋 가정이 맞는지 즉시 드러납니다.

- [x] 공식 앱과의 기존 연결을 완전히 해제했다.
- [x] Service/Characteristic UUID를 실제 GATT에서 확인했다. (2026-08-06, 일치)
- [x] 패킷 길이(18)와 부호·숫자 오프셋을 확인했다. (일치 — 단위만 0.01 g로 정정)
- [x] 음수 무게(영점 후 물건 제거)를 확인했다. (index 2 → `0x2D`, −341.1 g 캡처)
- [x] 빈 저울(0 g) raw hex를 기록했다.
- [x] notify 주기를 측정했다. **평균 9.3 Hz** (약 107 ms 간격)
- [x] 명령 5종이 모두 동작했다. (`T` 영점 / `R` 시작 / `S` 정지 / `C` 리셋 / `2` 모드)
- [ ] 연결 끊김·자동 재연결 동작을 확인했다. (Phase 1 구현 시)
- [ ] 비-무게 패킷의 구조를 분석했다. (현재 불필요 — 헤더로 걸러내면 충분)
- [ ] **타이머 값이 패킷에 실려 오는지 확인했다.** (아래 참고)

## 타이머 값 찾기 (미해결)

앱에서 저울로 타이머를 켜는 방향(`R`·`S`·`C`)은 동작합니다.
**반대 방향 — 저울의 타이머 버튼을 눌렀을 때 앱이 아는 것 — 은 아직 안 됩니다.**
index 12·13·15가 패킷마다 변하지만 무엇인지 확인한 적이 없기 때문입니다.

타이머가 실려 온다면 어떤 자리가 **시간에 비례해 일정하게 커집니다** — 초 단위면 1초에 +1,
0.1초 단위면 1초에 +10. [`tools/hex-dump.html`](../tools/hex-dump.html)의 **"타이머 바이트 찾기"**가
이 규칙으로 자리를 자동 판정합니다.

1. 저울을 연결하고 명령 `2`(weight+timer 모드)를 한 번 보냅니다
2. 저울 타이머를 정지·리셋해 0으로 맞춥니다
3. **추적 시작**을 누릅니다
4. **저울의 타이머 버튼을 직접 누르고** 20초 이상 둡니다
5. `타이머 후보`로 표시된 자리를 이 문서에 적습니다

무게처럼 오르내리는 자리는 자동으로 걸러집니다. 후보가 하나도 안 나오면
**저울이 타이머를 보내지 않는 것**이므로, 저울 버튼으로 앱을 시작하는 기능은 포기하고
다른 방식(무게가 오르면 자동 시작)을 씁니다.

**샘플링 주기 근거** — 저울이 약 9.3 Hz(107 ms)로 보내므로 [`api.md`](api.md)의 100 ms 샘플링 설계가 타당합니다.
저울보다 촘촘히 샘플링해도 새 값이 없으므로, 실제 해상도는 저울의 전송 주기가 상한입니다.

